---
import Prose from "@/components/Content/Prose.astro";
import SidebarPage from "@/layouts/SidebarPage.astro";
import { getEntry } from "astro:content";

const entry = await getEntry("misc", `en/presskit`);
if (!entry) return;
const { Content } = await entry.render();
---

<SidebarPage
    sidebarMode="guides"
    meta={{
        title: "KAPLAY, Presskit",
        description: "KAPLAY is a JavaScript game library that makes it easy to create games. Is the successor of KABOOM.JS.",
        keywords: "kaplay, kaboom, game, library, javascript, easy, simple",
    }}
>
    <Prose>
        <Content />
    </Prose>
</SidebarPage>
