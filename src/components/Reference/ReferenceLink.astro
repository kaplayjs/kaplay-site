---
type Props = {
    href: string;
    styled?: boolean;
};

const { href, styled } = Astro.props;
---

<a
    class="type-btn"
    class:list={{
        "decoration-current underline decoration-dashed": styled,
    }}><slot /><!-- --></a
>
