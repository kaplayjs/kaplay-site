---
import ReferenceEntry from "../ReferenceEntry.astro";

const { data } = Astro.props;
const members = data.members;
const parentName = data.name;

if (!members) return;
---

{
    Object.keys(members).map((member: any) => {
    const membersData = members[member];

    return membersData.map((memberData: any) => {
        return <ReferenceEntry data={memberData} parent={`${parentName}-`} />;
        });
    })
}
