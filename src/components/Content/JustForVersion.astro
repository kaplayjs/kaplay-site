---
import isVersion from "../../util/isVersion";

interface Props {
    version: string;
    requireToBeEqualIn?: "major" | "minor" | "patch";
}

let { version, requireToBeEqualIn = "major" } = Astro.props;
---

{isVersion(version, requireToBeEqualIn) && <slot />}
