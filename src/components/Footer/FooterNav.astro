---
type Props = {
    title: string;
};

const { title } = Astro.props;
---

<nav class="flex flex-col">
    <h3 class="text-lg font-semibold uppercase text-primary">{title}</h3>

    <slot />
</nav>
