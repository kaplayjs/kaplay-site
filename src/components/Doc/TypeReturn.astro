---
import TypeGenerics from "./TypeGenerics.astro";
import TypeSig from "./TypeSig.astro";

const { data } = Astro.props;

if (data?.name === "kaplay") {
    return null;
}

const typeData = data?.type?.type ?? data?.type;
---

:{" "}<TypeSig data={typeData} /><TypeGenerics data={typeData} />
